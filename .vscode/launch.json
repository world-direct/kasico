{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Operator",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/operator",
            "cwd": "${workspaceFolder}/operator",
            "env": {
                "WATCH_NAMESPACE": "kasico-operator",
            }            
        }
    ]
}