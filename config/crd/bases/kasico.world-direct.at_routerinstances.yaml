---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: routerinstances.kasico.world-direct.at
spec:
  group: kasico.world-direct.at
  names:
    kind: RouterInstance
    listKind: RouterInstanceList
    plural: routerinstances
    singular: routerinstance
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RouterInstance is the Schema for the routerinstances API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RouterInstanceSpec defines the desired state of RouterInstance
            properties:
              ingressClassName:
                description: IngressClassName is the name of the ingressClass managed
                  by this RouterInstance.
                type: string
              routerService:
                description: RouterService defines configuration values for the generated
                  service
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations allows the user to add annoations to
                      the router service
                    type: object
                type: object
            type: object
          status:
            description: RouterInstanceStatus defines the observed state of RouterInstance
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
